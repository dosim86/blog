<script>
    window.userActiveInterval={{ is_granted('ROLE_USER') ? user_active_interval : 0 }};
    {% if app.user %}
        window.user = JSON.parse('{{ app.user|json }}');
    {% endif %}
</script>