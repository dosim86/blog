{% extends 'base.html.twig' %}

{% block title %}{{ article.title }}{% endblock %}

{% block body %}
    <div class="container">
        <a href="{{ path('article_index') }}" class="btn btn-light">Back</a>
        <a href="{{ path('article_edit', {id:article.id}) }}" class="btn btn-success">Edit</a>
        <hr>
        <h1>{{ article.title }}</h1>
        <p>
            {{ article.content }}
        </p>
    </div>
{% endblock %}