{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            {% for blog in blogs %}
                <div class="col-md-4">
                    <h2>{{ blog.title }}</h2>
                    <p>{{ blog.content }}</p>
                    <p><a class="btn btn-secondary" href="#" role="button">View details »</a></p>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
